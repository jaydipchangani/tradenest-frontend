import React from 'react';
import { Typography } from 'antd';

const { Title, Paragraph } = Typography;

const Register: React.FC = () => {
  return (
    <div style={{ maxWidth: 400, margin: 'auto', marginTop: 80, padding: 20 }}>
      <Title level={3}>TradNest Register</Title>
      <Paragraph>Registration page is under construction.</Paragraph>
    </div>
  );
};

export default Register;
